# traccia

Invia una richiesta POST con l'header Content-Type: application/json e un body con title e userId. Nel primo .then() leggi e stampa il valore dell'header Content-Type della risposta, poi restituisci i dati come JSON. Nel secondo .then() stampa il titolo del post creato.

api:https://jsonplaceholder.typicode.com/posts